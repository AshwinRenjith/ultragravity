# Unit Tests

Use this folder for isolated logic tests:
- policy and permission rules
- parsing and schema validation
- utility and deterministic logic

Unit tests should not require external APIs or full UI automation.
