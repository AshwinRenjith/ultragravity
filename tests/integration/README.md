# Integration Tests

Use this folder for cross-module workflows:
- planner to executor handoff
- provider budget gating paths
- safety gateway and audit interactions

Integration tests may mock external providers while validating end-to-end module contracts.
